<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quara All Posts</title>
    <link rel="stylesheet" href="/style.css"> 
</head>
<body>
    <h1 class="title">Quara Posts</h1>
    <% for(let post of posts){ %>
        <div class="post">
            <h3 class="user">@<%= post.username %>
            </h3>
            <p class="content">
                <%= post.content %>
            </p>
            <a href="http://localhost:8080/posts/<%= post.id %>" class="btn">See Details</a>
            <a href="http://localhost:8080/posts/<%= post.id %>/edit" class="btn">Edit</a>
            <form action="/posts/<%= post.id %>?_method=DELETE" method="post" class="delete">
                <button class="btn">Delete</button>
            </form>
        </div>
    <% } %>
            <br><br>
            <a href="http://localhost:8080/posts/new" class="btn">Create a new post</a>
</body>

</html>